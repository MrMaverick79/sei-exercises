
<h2>Showing Details for "<%= @work.title %>"</h2>

    <%= link_to 'Edit', edit_work_path %>
    |
     <%= link_to 'Delete', work_path(@work.id), method: "delete" %>

<%= image_tag @work.image, alt: "Head in the clouds"  %>

<p><strong>Year:</strong><%= @work.year %></p>
<p><strong>Medium:</strong><%= @work.medium %></p>
<p><strong>Style:</strong><%= @work.style %></p>

<p> By<%= link_to @work.artist.name, artist_path(@work.artist.id) %> </p>



<%# .text "title"
    t.string "year"
    t.string "medium"
    t.text "style"
    t.text "image"
    t.datetime "created_at", null: false
    t.datetime "updated_at", null: false
    t.integer "artist_id" %>